{Твои одноклассники попросили тебя скопировать для них кое-какие документы.
Вы знаете, что есть "n" одноклассников, а в документах есть " m " страниц.
Ваша задача-рассчитать, сколько пустых страниц вам нужно.
Если n < 0 или m < 0, верните 0.
Пример:
n= 5, m=5: 25
n=-5, m=5: 0}

program pop;
uses Crt;
var
    n, m, sum :Integer;
begin
ReadLn(n);
readln(m);

    if (n < 0) or (m < 0) then 
    sum := 0
     
   
   
    else
    sum := n*m;

WriteLn(sum);
Readln;

end.
